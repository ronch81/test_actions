name: My First WorkFlow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout
          uses: actions/checkout@v4

        - name: My First Step
          run: echo "My First GitHubs Actions Job"

        - name: Query List of files in repo
          run: ls -lh
          
        - name: Show contents of README.MD
          run: cat README.md


